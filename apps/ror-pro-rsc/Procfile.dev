# Procfile for development
# You can run these commands in separate shells
#
# To run multiple worktrees simultaneously, set different ports in each worktree's .env:
#   PORT=3001
#   SHAKAPACKER_DEV_SERVER_PORT=3036
# Skip validation for yalc development (both react-on-rails and react-on-rails-pro installed)
rails: REACT_ON_RAILS_SKIP_VALIDATION=true bundle exec rails s -p ${PORT:-3000}
dev-server: bin/shakapacker-dev-server
server-bundle: SERVER_BUNDLE_ONLY=true bin/shakapacker-watch --watch

# React on Rails Pro - Node Renderer for SSR
node-renderer: RENDERER_LOG_LEVEL=debug RENDERER_PORT=3800 node renderer/node-renderer.js

# React on Rails Pro - RSC bundle watcher
rsc-bundle: RSC_BUNDLE_ONLY=true bin/shakapacker-watch --watch
